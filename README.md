## Reown AppKit Stable Testnet Example

Minimal React + Vite example using:
- `@reown/appkit` + `@reown/appkit-adapter-wagmi`
- Custom Stable Testnet chain (id 2201)
- Native USDT0 transfer button

### Run

```bash
npm install
npm run dev
```
